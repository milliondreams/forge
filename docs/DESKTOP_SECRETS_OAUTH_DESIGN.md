# Desktop Secrets and OAuth Design

> **Superseded:** This pre-implementation proposal is retained only as a
> durable pointer. It is not part of the Forge v1 contract.

The implemented credential ownership, structured requirement schema,
mandatory launch preflight API, remediation flow, and least-privilege process
injection behavior are defined in
[Agent Credential Needs and Launch Preflight](AGENT_NEEDS_DESIGN.md).
